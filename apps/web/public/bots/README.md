Bot portraits go here, named `<id>.png` to match `deployment/config/bots.yml`.

The UI falls back to the character's initial when a file is missing, so art can
land one band at a time without breaking the bot list.

See `BOT_ART_SHEET.md` at the repo root for the generation brief.
